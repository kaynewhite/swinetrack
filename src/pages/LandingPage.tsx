export default function LandingPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p>Landing page placeholder — design subagent will replace this.</p>
    </div>
  );
}
