export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p>Login page placeholder — design subagent will replace this.</p>
    </div>
  );
}
